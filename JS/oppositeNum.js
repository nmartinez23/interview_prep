// Create a function that takes a number and returns the opposite
// number. If number is positive it should return the same number
// as negative...and vice versa.

function oppositeNumber(n) {
  var result = 0 - n;

  return result;
};

var answer = oppositeNumber(7);
console.log(answer);
